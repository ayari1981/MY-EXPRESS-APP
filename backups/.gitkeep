# Backups Directory

هذا المجلد يحتوي على النسخ الاحتياطية لقاعدة البيانات بصيغة JSON.

## ملاحظات:
- لا تُرفع هذه الملفات إلى Git (تحتوي على بيانات حساسة)
- تُحفظ النسخ الاحتياطية تلقائياً عند تشغيل `scripts/export_database.js`
- احتفظ بنسخ محلية في مكان آمن

## الملفات:
- `backup_YYYY-MM-DDTHH-MM-SS.json` - نسخة احتياطية كاملة

---
*تم إنشاء هذا المجلد تلقائياً*
