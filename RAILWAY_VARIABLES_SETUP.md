# إعداد متغيرات Railway - خطوة بخطوة

## المشكلة الحالية
التطبيق يستخدم `localhost` على Railway لأن **المتغيرات غير موجودة في لوحة Railway**.

## الحل (اختر طريقة واحدة)

### الطريقة 1: MYSQL_URL (الأسرع - متغير واحد فقط)

1. افتح لوحة Railway: https://railway.app
2. اختر مشروعك `MY-EXPRESS-APP`
3. اضغط على الخدمة (Service)
4. اذهب إلى تبويب **Variables**
5. اضغط **New Variable**
6. املأ:
   - **Variable Name:** `MYSQL_URL`
   - **Value:** `mysql://root:SWRwvEsAmiQYoxmesxpxOulHjwfeYUzt@metro.proxy.rlwy.net:51425/railway`
7. اضغط **Add**
8. سيعيد Railway النشر تلقائياً

---

### الطريقة 2: متغيرات منفصلة (DB_*)

إذا فضلت متغيرات منفصلة، أضف هذه الخمسة:

| Variable Name | Value |
|--------------|-------|
| `DB_HOST` | `metro.proxy.rlwy.net` |
| `DB_PORT` | `51425` |
| `DB_NAME` | `railway` |
| `DB_USER` | `root` |
| `DB_PASSWORD` | `SWRwvEsAmiQYoxmesxpxOulHjwfeYUzt` |

---

## التحقق من النجاح

بعد إعادة النشر، افتح **Logs** ويجب أن ترى:

```
✅ MySQL متصل بنجاح
📊 قاعدة البيانات: railway
🗄️ المضيف: metro.proxy.rlwy.net:51425
✅ تم مزامنة النماذج مع قاعدة البيانات
🚀 الخادم يعمل على المنفذ 8080
```

### إذا رأيت هذا، فالمتغيرات **لم تُضَف**:
```
❌ خطأ في الاتصال بقاعدة البيانات
HOST=localhost PORT=3306 DB=ecole_chebbi
```

---

## ملاحظات مهمة

1. **Railway لا يقرأ `.env`** - يجب إضافة المتغيرات يدوياً في Variables
2. **لا تحذف `.env` من المشروع** - مطلوب للتطوير المحلي
3. **تأكد من عدم وجود مسافات** في القيم
4. **الأحرف الكبيرة مهمة**: `MYSQL_URL` وليس `mysql_url`

---

## إذا لم ينجح

1. تحقق من أن قاعدة البيانات Railway تعمل:
   ```bash
   node scripts/test_database_connection.js
   ```

2. تأكد من إضافة المتغيرات **في الخدمة الصحيحة** (Web Service وليس Database)

3. أرسل لقطة شاشة من:
   - صفحة Variables
   - آخر 20 سطر من Logs
