# 🚀 دليل نشر التطبيق على الاستضافة

## 📋 الخطوات الكاملة لنقل التطبيق من المحلي إلى الاستضافة

---

## 1️⃣ تصدير قاعدة البيانات المحلية

### الطريقة الأولى: باستخدام phpMyAdmin (الأسهل)

1. **افتح phpMyAdmin:**
   - اذهب إلى: `http://localhost/phpmyadmin`
   
2. **اختر قاعدة البيانات:**
   - اضغط على `ecole_chebbi` من القائمة اليسرى

3. **تصدير البيانات:**
   - اضغط على تبويب **"Export"** أو **"تصدير"**
   - اختر **"Quick"** للتصدير السريع
   - أو اختر **"Custom"** للخيارات المتقدمة:
     - Format: **SQL**
     - تأكد من تفعيل:
       - ✅ Structure
       - ✅ Data
       - ✅ CREATE DATABASE / USE statement
   - اضغط **"Go"** أو **"تنفيذ"**
   
4. **حفظ الملف:**
   - سيتم تحميل ملف: `ecole_chebbi.sql`
   - احفظه في مكان آمن

### الطريقة الثانية: باستخدام Terminal (متقدم)

```bash
# تصدير قاعدة البيانات مع البيانات
mysqldump -u root -p ecole_chebbi > ecole_chebbi_backup.sql

# تصدير مع ضغط
mysqldump -u root -p ecole_chebbi | gzip > ecole_chebbi_backup.sql.gz
```

---

## 2️⃣ استيراد قاعدة البيانات على الاستضافة

### في cPanel (معظم الاستضافات):

1. **سجل دخول cPanel**

2. **اذهب إلى MySQL Databases:**
   - ابحث عن "MySQL Databases"
   - انقر عليه

3. **إنشاء قاعدة بيانات جديدة:**
   - اسم قاعدة البيانات: `ecole_chebbi`
   - اضغط "Create Database"
   - **احفظ الاسم الكامل** (مثل: `username_ecole_chebbi`)

4. **إنشاء مستخدم قاعدة البيانات:**
   - Username: `ecole_user`
   - Password: **كلمة مرور قوية** (احفظها!)
   - اضغط "Create User"

5. **ربط المستخدم بقاعدة البيانات:**
   - اختر المستخدم وقاعدة البيانات
   - اختر **"ALL PRIVILEGES"**
   - اضغط "Add"

6. **استيراد البيانات عبر phpMyAdmin:**
   - اذهب إلى phpMyAdmin في cPanel
   - اختر قاعدة البيانات `username_ecole_chebbi`
   - اضغط تبويب **"Import"**
   - اختر ملف `ecole_chebbi.sql`
   - اضغط **"Go"**

### في استضافات أخرى:

```bash
# إذا كان لديك SSH access
mysql -u username -p database_name < ecole_chebbi_backup.sql
```

---

## 3️⃣ رفع ملفات التطبيق

### باستخدام FileZilla أو FTP:

1. **اتصل بالخادم:**
   - Host: `ftp.yourdomain.com`
   - Username: من معلومات الاستضافة
   - Password: من معلومات الاستضافة
   - Port: 21

2. **ارفع الملفات:**
   - ارفع كل ملفات المشروع **ما عدا:**
     - ❌ `node_modules/`
     - ❌ `.git/`
     - ❌ `uploads/` (إلا إذا كانت تحتوي بيانات مهمة)

3. **الملفات المطلوبة:**
   - ✅ `src/`
   - ✅ `views/`
   - ✅ `public/`
   - ✅ `package.json`
   - ✅ `.env` (سنعدله)
   - ✅ جميع الملفات الأخرى

---

## 4️⃣ تحديث ملف .env للاستضافة

أنشئ ملف `.env` جديد على الاستضافة:

```env
# Database Configuration - Production
DB_HOST=localhost
DB_PORT=3306
DB_NAME=username_ecole_chebbi
DB_USER=username_ecole_user
DB_PASSWORD=your_strong_password_here

# Server Configuration
PORT=3000
NODE_ENV=production

# Session Secret - غيّر هذا المفتاح!
SESSION_SECRET=غيّر-هذا-المفتاح-إلى-شيء-عشوائي-طويل-ومعقد-2024

# File Upload
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=.pdf,.docx,.doc,.pptx,.ppt

# Admin Credentials
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=admin_password_change_this
```

⚠️ **مهم جداً:**
- غيّر `DB_NAME` و `DB_USER` و `DB_PASSWORD` بالقيم الصحيحة من الاستضافة
- غيّر `SESSION_SECRET` إلى شيء عشوائي ومعقد
- غيّر `ADMIN_EMAIL` إلى بريدك الحقيقي

---

## 5️⃣ تثبيت Node.js على الاستضافة

### في cPanel مع Node.js Support:

1. **اذهب إلى "Setup Node.js App"**

2. **إنشاء تطبيق جديد:**
   - Node.js version: **18.x** أو أحدث
   - Application mode: **Production**
   - Application root: `/home/username/public_html/my-express-app`
   - Application URL: `yourdomain.com`
   - Application startup file: `src/app.js`

3. **حفظ وتشغيل:**
   - اضغط "Create"
   - سيتم تثبيت `node_modules` تلقائياً

### عبر SSH:

```bash
# الدخول للمجلد
cd /path/to/your/app

# تثبيت الحزم
npm install --production

# تشغيل التطبيق
npm start

# أو باستخدام PM2 (موصى به)
npm install -g pm2
pm2 start src/app.js --name ecole-app
pm2 save
pm2 startup
```

---

## 6️⃣ إعداد Nginx أو Apache (اختياري)

### Nginx Reverse Proxy:

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /uploads {
        alias /path/to/app/uploads;
    }

    location /public {
        alias /path/to/app/public;
    }
}
```

### Apache .htaccess:

```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^$ http://127.0.0.1:3000/ [P,L]
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ http://127.0.0.1:3000/$1 [P,L]
</IfModule>
```

---

## 7️⃣ إنشاء حساب المدير

بعد تشغيل التطبيق:

```bash
npm run create-admin
```

أو قم بإنشاء حساب مدير يدوياً من صفحة التسجيل.

---

## 8️⃣ التحقق من عمل التطبيق

1. **افتح المتصفح:**
   - اذهب إلى: `http://yourdomain.com`

2. **اختبر الصفحات:**
   - ✅ الصفحة الرئيسية
   - ✅ تسجيل الدخول
   - ✅ التسجيل
   - ✅ لوحة التحكم

3. **اختبر الميزات:**
   - ✅ رفع درس (أستاذ)
   - ✅ عرض الدروس (تلميذ)
   - ✅ تحميل الملفات
   - ✅ التعليقات

---

## 9️⃣ استضافات موصى بها

### 🌟 استضافات تدعم Node.js:

1. **Heroku** (مجاني للبداية)
   - سهل جداً
   - يدعم MySQL
   - نشر تلقائي من Git

2. **Railway.app** (مجاني)
   - بسيط وسريع
   - MySQL مدمج
   - نشر من GitHub

3. **DigitalOcean** ($5/شهر)
   - خادم كامل
   - تحكم كامل
   - أداء ممتاز

4. **Vercel** (مجاني)
   - سريع جداً
   - لكن يحتاج قاعدة بيانات خارجية

5. **Namecheap Shared Hosting** ($3/شهر)
   - دعم Node.js
   - cPanel
   - مناسب للمبتدئين

---

## 🔟 النشر على Railway (الأسهل والأسرع)

### الخطوات:

1. **إنشاء حساب:**
   - اذهب إلى: https://railway.app
   - سجل دخول بـ GitHub

2. **إنشاء مشروع جديد:**
   - اضغط "New Project"
   - اختر "Deploy from GitHub repo"
   - اختر المستودع

3. **إضافة قاعدة بيانات:**
   - اضغط "+ New"
   - اختر "Database" → "MySQL"
   - سيتم إنشاؤها تلقائياً

4. **ربط المتغيرات:**
   - Railway سيضبط متغيرات البيئة تلقائياً
   - أو أضفها يدوياً من `.env`

5. **النشر:**
   - Railway سينشر تلقائياً!
   - احصل على الرابط

---

## 📝 ملاحظات مهمة:

### ✅ قبل النشر:

- [ ] نسخ احتياطي من قاعدة البيانات
- [ ] اختبار التطبيق محلياً
- [ ] تحديث `SESSION_SECRET`
- [ ] تغيير كلمة مرور المدير
- [ ] مراجعة ملف `.env`
- [ ] حذف البيانات التجريبية

### ⚠️ الأمان:

- استخدم HTTPS (SSL Certificate)
- غيّر جميع كلمات المرور الافتراضية
- فعّل firewall على الخادم
- قيّد الوصول لـ phpMyAdmin
- احذف ملفات الاختبار

### 🔄 التحديثات المستقبلية:

```bash
# على جهازك المحلي
git add .
git commit -m "تحديث جديد"
git push origin main

# على الخادم (SSH)
git pull origin main
npm install
pm2 restart ecole-app
```

---

## 🆘 حل المشاكل الشائعة:

### المشكلة: "Cannot connect to database"
**الحل:**
- تحقق من `DB_HOST`, `DB_USER`, `DB_PASSWORD` في `.env`
- تأكد من تشغيل MySQL
- تحقق من صلاحيات المستخدم

### المشكلة: "node_modules not found"
**الحل:**
```bash
npm install --production
```

### المشكلة: "Port 3000 already in use"
**الحل:**
- غيّر البورت في `.env`: `PORT=3001`
- أو أوقف العملية الأخرى

### المشكلة: "Files not uploading"
**الحل:**
- تحقق من صلاحيات مجلد `uploads/`: `chmod 755 uploads`
- تحقق من حجم الملف المسموح في الخادم

---

## ✅ قائمة التحقق النهائية:

- [ ] قاعدة البيانات تعمل
- [ ] الملفات مرفوعة
- [ ] `.env` محدّث
- [ ] `node_modules` مثبتة
- [ ] التطبيق يعمل
- [ ] حساب المدير منشأ
- [ ] جميع الصفحات تعمل
- [ ] رفع الملفات يعمل
- [ ] SSL مفعّل (HTTPS)
- [ ] نسخة احتياطية جاهزة

---

## 🎉 مبروك!

الآن تطبيق المدرسة منشور ويعمل على الإنترنت! 🚀

للدعم، راجع:
- `DEPLOYMENT_RAILWAY.md` - دليل النشر على Railway
- `DEPLOYMENT_HEROKU.md` - دليل النشر على Heroku
- `DATABASE_EXPORT.md` - تصدير واستيراد قاعدة البيانات
