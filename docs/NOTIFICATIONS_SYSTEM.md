# نظام إشعارات الغيابات والعقوبات

## ✅ تم إنشاء النظام بنجاح

### الميزات المُنفذة:

#### 1. نموذج قاعدة البيانات (StudentRecord)
- **الجدول**: `student_records`
- **الحقول**:
  - `recordType`: نوع السجل (غياب / عقوبة / ملاحظة)
  - `absenceType`: نوع الغياب (مبرر / غير مبرر)
  - `punishmentType`: نوع العقوبة (إنذار / توقيف / طرد مؤقت / أخرى)
  - `punishmentSeverity`: درجة الخطورة (بسيطة / متوسطة / شديدة)
  - `description`: الوصف التفصيلي
  - `notes`: ملاحظات إضافية
  - `date`: تاريخ الواقعة
  - `recordedBy`: المعلم/الإدارة الذي سجل
  - `parentNotified`: هل تم إشعار الولي
  - `isRead`: هل قرأ الولي الإشعار
  - `notifiedAt`: تاريخ الإشعار
  - `readAt`: تاريخ القراءة

#### 2. مسارات المعلم (Teacher Routes)
- **GET** `/teacher/records` - عرض جميع السجلات المُسجلة
- **GET** `/teacher/records/add` - صفحة إضافة سجل جديد
- **POST** `/teacher/records/add` - إضافة سجل جديد
- **POST** `/teacher/records/edit/:id` - تعديل سجل
- **POST** `/teacher/records/delete/:id` - حذف سجل

**الفلاتر المتاحة**:
- القسم (السابعة/الثامنة/التاسعة أساسي)
- رقم الصف (1-10)
- نوع السجل (غياب/عقوبة/ملاحظة)
- التاريخ (من - إلى)

#### 3. مسارات الولي (Parent Routes)
- **GET** `/parent/notifications` - عرض جميع إشعارات الابن
- **POST** `/parent/notifications/mark-read/:id` - تعليم إشعار واحد كمقروء
- **POST** `/parent/notifications/mark-all-read` - تعليم جميع الإشعارات كمقروءة

**الفلاتر المتاحة**:
- نوع السجل (غياب/عقوبة/ملاحظة)
- الحالة (مقروء/غير مقروء)
- التاريخ (من - إلى)

#### 4. واجهات المستخدم (Views)

**للمعلم**:
- `views/teacher/records.ejs` - قائمة السجلات مع إحصائيات شاملة
- `views/teacher/records-add.ejs` - نموذج إضافة سجل جديد

**للولي**:
- `views/parent/notifications.ejs` - قائمة الإشعارات مع إحصائيات وفلاتر

#### 5. التكامل مع لوحات التحكم
- **لوحة المعلم**: تمت إضافة زر "سجلات الطلاب" (برتقالي)
- **لوحة الولي**: تمت إضافة بطاقة "إشعارات الابن" (برتقالي)

### الوظائف الرئيسية:

#### للمعلم:
1. **إضافة سجل غياب**:
   - اختيار القسم ورقم الصف
   - اختيار الطالب
   - تحديد نوع الغياب (مبرر/غير مبرر)
   - إضافة وصف وملاحظات

2. **إضافة سجل عقوبة**:
   - اختيار الطالب
   - تحديد نوع العقوبة (إنذار/توقيف/طرد/أخرى)
   - تحديد درجة الخطورة (بسيطة/متوسطة/شديدة)
   - إضافة وصف تفصيلي

3. **إضافة ملاحظة**:
   - تسجيل ملاحظة عامة عن الطالب
   - إضافة وصف وملاحظات

4. **الإشعار التلقائي**:
   - عند إضافة سجل جديد، يتم إشعار الولي تلقائياً
   - يظهر الإشعار في قسم "إشعارات الابن" للولي

#### للولي:
1. **عرض الإشعارات**:
   - قائمة شاملة بجميع الغيابات والعقوبات
   - تمييز الإشعارات غير المقروءة بلون مختلف وشارة "جديد"
   - عرض التفاصيل الكاملة لكل سجل

2. **الإحصائيات**:
   - إجمالي السجلات
   - عدد الإشعارات غير المقروءة
   - عدد الغيابات
   - عدد العقوبات

3. **التصفية والبحث**:
   - فلترة حسب نوع السجل
   - فلترة حسب الحالة (مقروء/غير مقروء)
   - فلترة حسب التاريخ

4. **تعليم كمقروء**:
   - تعليم إشعار واحد كمقروء
   - تعليم جميع الإشعارات كمقروءة دفعة واحدة

### متطلبات الاستخدام:

#### للولي:
- يجب أن يكون الحساب **مربوطاً بالابن** لرؤية السجلات
- طرق الربط:
  1. **أثناء التسجيل**: إدخال الاسم الأول والأخير للابن
  2. **عن طريق الإدارة**: طلب ربط الحساب من الإدارة
  3. **البحث المؤقت** (للنتائج فقط): البحث باسم الابن الكامل

### الملفات المُنشأة:

```
src/
  models/
    StudentRecord.js                  ✅ نموذج قاعدة البيانات
  routes/
    teacher.js                        ✅ تم تحديث (إضافة مسارات السجلات)
    parent.js                         ✅ تم تحديث (إضافة مسارات الإشعارات)

views/
  teacher/
    records.ejs                       ✅ صفحة عرض السجلات
    records-add.ejs                   ✅ صفحة إضافة سجل
    dashboard.ejs                     ✅ تم تحديث (إضافة زر)
  parent/
    notifications.ejs                 ✅ صفحة الإشعارات
    dashboard.ejs                     ✅ تم تحديث (إضافة بطاقة)

database/
  create_student_records_table.sql  ✅ سكريبت SQL
  migrate_student_records.js        ✅ سكريبت الهجرة
```

### قاعدة البيانات:
- ✅ تم إنشاء الجدول `student_records` في قاعدة بيانات Railway
- ✅ تم تعريف جميع العلاقات مع جدول `users`
- ✅ تم إنشاء 8 فهارس لتحسين الأداء

### الخادم:
- ✅ يعمل على المنفذ 3000
- ✅ متصل بقاعدة بيانات Railway بنجاح
- ✅ تم مزامنة جميع النماذج

### الروابط المباشرة:

#### للمعلم:
- لوحة التحكم: `http://localhost:3000/teacher/dashboard`
- السجلات: `http://localhost:3000/teacher/records`
- إضافة سجل: `http://localhost:3000/teacher/records/add`

#### للولي:
- لوحة التحكم: `http://localhost:3000/parent/dashboard`
- الإشعارات: `http://localhost:3000/parent/notifications`

---

## 🎯 النظام جاهز للاستخدام!

يمكنك الآن:
1. تسجيل الدخول كمعلم وإضافة سجلات للطلاب
2. تسجيل الدخول كولي ومتابعة إشعارات الابن
3. تصفية وترتيب السجلات حسب احتياجك
4. تعليم الإشعارات كمقروءة بعد الاطلاع عليها
