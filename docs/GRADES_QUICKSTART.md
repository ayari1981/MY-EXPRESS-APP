# دليل البدء السريع - نظام النتائج

## 🚀 الخطوات الأولى

### 1️⃣ تشغيل SQL Migration
قم بتشغيل الأمر التالي لإنشاء الجداول في قاعدة البيانات:

```bash
# في PowerShell
Get-Content create_grades_table.sql | mysql -h metro.proxy.rlwy.net -P 51425 -u root -p railway
```

أو افتح Railway MySQL وقم بتنفيذ محتوى ملف `create_grades_table.sql` يدويًا.

### 2️⃣ تشغيل السيرفر
```bash
npm start
```

### 3️⃣ اختبار النظام

#### كمعلم:
1. سجل دخول بحساب معلم
2. اذهب إلى Dashboard → **إدارة النتائج**
3. انقر **إضافة نتائج**
4. اختر القسم: `السابعة أساسي`
5. املأ البيانات:
   - المادة: `الرياضيات`
   - نوع الفرض: `فرض عادي`
   - الفصل: `الفصل الأول`
6. أدخل العلامات للتلاميذ
7. احفظ → النتائج ستكون "غير منشورة"
8. ارجع لصفحة النتائج وانقر على أيقونة العين لنشر النتيجة

#### كتلميذ:
1. سجل دخول بحساب تلميذ
2. اذهب إلى Dashboard → **نتائجي**
3. سترى النتائج المنشورة فقط مع المعدل العام

#### كولي:
1. سجل دخول بحساب ولي
2. اذهب إلى Dashboard → **نتائج الابن/الابنة**
3. إذا لم يكن الحساب مرتبط بابن:
   - تواصل مع المسؤول
   - سيضيف اسم الابن في حسابك من إدارة المستخدمين

#### كمسؤول:
1. سجل دخول بحساب مسؤول
2. اذهب إلى Dashboard → **إدارة النتائج**
3. يمكنك:
   - عرض جميع النتائج
   - فلترة حسب القسم/المادة/الفصل/المعلم
   - عرض تقرير شامل لأي تلميذ
   - حذف أي نتيجة

## 📊 أنواع الفروض

- **فرض عادي**: امتحان عادي (من 20)
- **فرض تأليفي**: امتحان تأليفي/composition (من 20)
- **شفاهي**: امتحان شفهي (من 10 افتراضيًا)

## 🔄 سير العمل الموصى به

```
المعلم: إضافة نتائج (غير منشورة)
    ↓
المعلم: مراجعة النتائج
    ↓
المعلم: نشر النتائج
    ↓
التلاميذ/الأولياء: عرض النتائج
```

## ⚠️ ملاحظات مهمة

1. **للأولياء**: يجب أن يضيف المسؤول اسم الابن في حساب الولي من صفحة "إدارة المستخدمين"
2. **للمعلمين**: النتائج غير المنشورة لا يراها التلاميذ أو الأولياء
3. **العلامات**: يجب أن تكون بين 0 و 20 (أو 10 للشفاهي)
4. **حذف النتائج**: فقط المعلم صاحب النتيجة أو المسؤول يمكنهم حذفها

## 🎨 الواجهة

جميع الصفحات مصممة بـ:
- ✅ تصميم عصري متجاوب (Responsive)
- ✅ دعم كامل للغة العربية (RTL)
- ✅ ألوان واضحة للتمييز بين أنواع الفروض
- ✅ إحصائيات تفاعلية
- ✅ فلاتر بحث متقدمة

## 🔧 استكشاف الأخطاء

### المشكلة: "لا توجد نتائج"
**الحل**: تأكد أن المعلم قام بنشر النتائج (النتائج غير المنشورة لا تظهر للتلاميذ)

### المشكلة: الولي لا يرى نتائج الابن
**الحل**: تأكد أن المسؤول أضاف اسم الابن في حساب الولي:
1. سجل دخول كمسؤول
2. إدارة المستخدمين
3. اختر حساب الولي → تعديل
4. املأ حقلي "اسم الابن" و "لقب الابن"

### المشكلة: خطأ عند إضافة نتائج
**الحل**: تأكد من:
- العلامة بين 0 و 20
- جميع الحقول المطلوبة (*) تم ملؤها
- القسم المختار يحتوي على تلاميذ

## 📁 الملفات المهمة

### Backend
- `src/models/Grade.js` - نموذج النتائج
- `src/routes/teacher.js` - مسارات المعلم
- `src/routes/student.js` - مسارات التلميذ
- `src/routes/parent.js` - مسارات الولي
- `src/routes/admin.js` - مسارات المسؤول

### Frontend
- `views/teacher/grades.ejs` - إدارة نتائج المعلم
- `views/teacher/grades-add.ejs` - إضافة نتائج
- `views/student/grades.ejs` - نتائج التلميذ
- `views/parent/grades.ejs` - نتائج الابن
- `views/admin/grades.ejs` - نظرة المسؤول
- `views/admin/student-grades.ejs` - تقرير تلميذ

### Database
- `create_grades_table.sql` - SQL migration

## 🎓 نصائح للاستخدام

### للمعلمين
- استخدم "إضافة نتائج جماعية" لتوفير الوقت
- راجع النتائج قبل النشر
- يمكنك نشر جميع نتائج فصل كامل دفعة واحدة
- استخدم حقل "ملاحظات" لإضافة تعليقات مفيدة

### للتلاميذ
- تحقق من نتائجك بانتظام
- استخدم فلتر "الفصل" لعرض نتائج فصل معين
- معدلك العام يُحسب تلقائيًا

### للأولياء
- تابع نتائج ابنك/ابنتك بشكل دوري
- اطلع على ملاحظات المعلمين
- تواصل مع المعلمين عند الحاجة

### للمسؤولين
- راقب الأداء العام للمدرسة
- استخدم الفلاتر المتقدمة للتحليل
- اعرض تقارير تفصيلية للتلاميذ

## 📞 الدعم

لمزيد من المعلومات، راجع الملف الكامل: `GRADES_SYSTEM.md`

---

✨ **جاهز للاستخدام!**
