# حل مشكلة عدم الدخول لفضاء التلميذ 🔧

## المشكلة:
التطبيق لا يعمل لأن **MySQL غير مثبت** على جهازك!

---

## الحل السريع (اختر واحد):

### ✅ الحل 1: تثبيت XAMPP (الأسهل - موصى به)

1. **تحميل XAMPP:**
   - اذهب إلى: https://www.apachefriends.org/download.html
   - حمّل XAMPP for Windows
   - ثبّت البرنامج

2. **تشغيل MySQL:**
   - افتح XAMPP Control Panel
   - اضغط "Start" بجانب MySQL
   - اضغط "Start" بجانب Apache (اختياري)

3. **إنشاء قاعدة البيانات:**
   - افتح المتصفح واذهب إلى: `http://localhost/phpmyadmin`
   - اضغط "New" في الشريط الجانبي
   - اسم قاعدة البيانات: `ecole_chebbi`
   - Collation: `utf8mb4_unicode_ci`
   - اضغط "Create"

4. **تحديث ملف .env:**
   ```env
   DB_PASSWORD=
   ```
   (اترك كلمة المرور فارغة مع XAMPP)

5. **تشغيل التطبيق:**
   ```bash
   npm start
   ```

---

### ✅ الحل 2: تثبيت MySQL مباشرة

1. **تحميل MySQL:**
   - https://dev.mysql.com/downloads/installer/
   - حمّل MySQL Installer for Windows

2. **التثبيت:**
   - شغّل المثبت
   - اختر "Developer Default"
   - اضبط كلمة مرور root: `loi123`
   - أكمل التثبيت

3. **إضافة MySQL للـ PATH:**
   ```powershell
   # افتح PowerShell كـ Administrator
   $env:Path += ";C:\Program Files\MySQL\MySQL Server 8.0\bin"
   ```

4. **إنشاء قاعدة البيانات:**
   ```bash
   mysql -u root -p"loi123"
   ```
   ثم:
   ```sql
   CREATE DATABASE ecole_chebbi CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   EXIT;
   ```

5. **تشغيل التطبيق:**
   ```bash
   npm start
   ```

---

## بعد تشغيل MySQL وإنشاء القاعدة:

### 1. تشغيل التطبيق:
```bash
npm start
```

يجب أن ترى:
```
✅ MySQL متصل بنجاح
📊 قاعدة البيانات: ecole_chebbi
✅ تم مزامنة النماذج مع قاعدة البيانات
🚀 الخادم يعمل على http://localhost:3000
```

### 2. إنشاء حساب مدير:
```bash
npm run create-admin
```

### 3. إنشاء حسابات تجريبية:

افتح المتصفح على: `http://localhost:3000/auth/register`

**إنشاء أستاذ:**
- الاسم: محمد الأستاذ
- البريد: teacher@test.com
- كلمة المرور: 123456
- الدور: teacher
- المادة: الرياضيات

**إنشاء تلميذ:**
- الاسم: أحمد التلميذ
- البريد: student@test.com
- كلمة المرور: 123456
- الدور: student
- القسم: السنة الثالثة
- الشعبة: أ

### 4. اختبار فضاء التلميذ:

1. سجل دخول كأستاذ وارفع درساً
2. سجل خروج
3. سجل دخول كتلميذ
4. اذهب إلى `/student/dashboard`
5. تصفح الدروس، حمّل، علّق!

---

## التحقق السريع:

```bash
# تحقق من MySQL
mysql --version

# تحقق من قاعدة البيانات
mysql -u root -p -e "SHOW DATABASES;"
```

---

## روابط فضاء التلميذ:

بعد تسجيل الدخول كتلميذ:
- لوحة التحكم: `http://localhost:3000/student/dashboard`
- جميع الدروس: `http://localhost:3000/student/lessons`
- الإشعارات: `http://localhost:3000/student/notifications`

---

## إذا ظهر خطأ "connection refused":
- تأكد من تشغيل MySQL
- في XAMPP: اضغط Start بجانب MySQL
- تحقق من البورت 3306

---

## ملاحظة مهمة:
**يجب تشغيل MySQL في كل مرة تريد استخدام التطبيق!**
- في XAMPP: افتح Control Panel واضغط Start لـ MySQL
- في MySQL Standalone: سيعمل تلقائياً كخدمة Windows

---

## المساعدة:
إذا واجهت أي مشكلة، راجع:
- `INSTALL_MYSQL.md` - دليل تثبيت MySQL التفصيلي
- `MYSQL_SETUP.md` - إعداد قاعدة البيانات
- `STUDENT_FEATURES.md` - ميزات فضاء التلميذ
