# تحديث نظام الإشعارات للمعلمين

## التغييرات الجديدة ✨

### 1️⃣ نظام إرسال متدرج (قسم → فصل → تلميذ)

تم تطوير نظام الإشعارات ليصبح أكثر مرونة وسهولة في الاستخدام:

#### **خيارات الإرسال الثلاثة:**

1. **إرسال لقسم كامل** 📚
   - اختر القسم (السابعة/الثامنة/التاسعة أساسي)
   - سيصل الإشعار لجميع تلاميذ القسم

2. **إرسال لفصل معين من قسم** 🎯
   - المرحلة 1: اختر القسم
   - المرحلة 2: اختر الفصل (يتم تحميله تلقائياً)
   - سيصل الإشعار لجميع تلاميذ الفصل المحدد فقط

3. **إرسال لتلميذ معين** 👤
   - المرحلة 1: اختر القسم
   - المرحلة 2: اختر الفصل (يتم تحميله تلقائياً)
   - المرحلة 3: اختر التلميذ من القائمة (يتم تحميلها تلقائياً)
   - سيصل الإشعار للتلميذ المحدد فقط

---

## التحديثات التقنية 🔧

### ملفات تم تعديلها:

#### 1. `src/routes/teacher.js`

##### API جديدة:

```javascript
// الحصول على الفصول المتاحة في قسم معين
GET /teacher/api/sections-by-class/:classLevel

// الحصول على التلاميذ في فصل معين
GET /teacher/api/students-by-section/:classLevel/:section
```

##### معالجة الإرسال:

- دعم 3 أنواع: `class`, `section`, `specific`
- التحقق من صحة البيانات
- رسائل نجاح مخصصة لكل نوع

#### 2. `views/teacher/send-notification.ejs`

##### واجهة محسّنة:

- 3 خيارات واضحة للإرسال
- اختيار متدرج (قسم → فصل → تلميذ)
- تحميل ديناميكي للبيانات عبر AJAX
- رسائل توجيهية تفاعلية
- معاينة مباشرة للإشعار

##### JavaScript تفاعلي:

- `toggleNotificationType()`: تبديل نوع الإرسال
- `loadSections()`: تحميل الفصول حسب القسم
- `loadStudents()`: تحميل التلاميذ حسب الفصل
- تحديث تلقائي للتلميحات والملاحظات

---

## كيفية الاستخدام 📖

### إرسال لقسم كامل:

1. اختر "إرسال لقسم كامل"
2. اختر القسم (مثلاً: السابعة أساسي)
3. اكتب عنوان ونص الإشعار
4. اضغط "إرسال الإشعار"

✅ سيصل لجميع تلاميذ السابعة أساسي

---

### إرسال لفصل معين:

1. اختر "إرسال لفصل معين من قسم"
2. اختر القسم (مثلاً: الثامنة أساسي)
3. انتظر تحميل الفصول ثم اختر الفصل (مثلاً: فصل 1)
4. اكتب عنوان ونص الإشعار
5. اضغط "إرسال الإشعار"

✅ سيصل لتلاميذ الثامنة أساسي - فصل 1 فقط

---

### إرسال لتلميذ محدد:

1. اختر "إرسال لتلميذ معين"
2. اختر القسم (مثلاً: التاسعة أساسي)
3. انتظر تحميل الفصول ثم اختر الفصل
4. انتظر تحميل التلاميذ ثم اختر التلميذ المستهدف
5. اكتب عنوان ونص الإشعار
6. اضغط "إرسال الإشعار"

✅ سيصل للتلميذ المحدد فقط

---

## المميزات الإضافية ⭐

### 1. تحميل ديناميكي
- لا حاجة لتحديث الصفحة
- الفصول تُحمّل تلقائياً عند اختيار القسم
- التلاميذ يُحمّلون تلقائياً عند اختيار الفصل

### 2. رسائل توجيهية واضحة
- "اختر القسم أولاً"
- "تم تحميل الفصول - اختر الفصل المستهدف"
- "المرحلة 1 مكتملة - الآن اختر الفصل"
- "جميع المراحل مكتملة - جاهز للإرسال"

### 3. التحقق من البيانات
- التحقق من اختيار جميع الحقول المطلوبة
- رسائل خطأ واضحة
- منع الإرسال إذا كانت البيانات ناقصة

### 4. ملاحظات سياقية
- ملاحظات مختلفة لكل نوع إرسال
- تحذيرات وتوجيهات مفيدة
- نصائح لإرسال إشعارات فعّالة

---

## اختبار النظام 🧪

### الرابط:
```
http://localhost:3000/teacher/send-notification
```

### حسابات التجربة:
- معلم: `teacher@ecole.tn` / كلمة المرور من قاعدة البيانات

---

## الدعم الفني 💡

إذا واجهت أي مشكلة:

1. تأكد من وجود تلاميذ في قاعدة البيانات
2. تحقق من تعيين `studentClass` و `section` للتلاميذ
3. افتح Console في المتصفح للتحقق من أخطاء JavaScript
4. راجع Logs السيرفر

---

## التحديثات المستقبلية 🚀

- [ ] إمكانية إرسال إشعار لعدة فصول
- [ ] جدولة إرسال الإشعارات
- [ ] قوالب إشعارات جاهزة
- [ ] تتبع من قرأ الإشعار ومن لم يقرأه
- [ ] إمكانية إرفاق ملفات مع الإشعار

---

تاريخ التحديث: 14 نوفمبر 2025
