# 🚀 دليل البدء السريع

## خطوات التشغيل السريع

### 1. تثبيت التبعيات
```bash
npm install
```

### 2. بناء CSS
```bash
npx tailwindcss -i ./public/css/input.css -o ./public/css/output.css
```

### 3. تشغيل MongoDB
**على Windows:**
```bash
net start MongoDB
```

**على Linux/Mac:**
```bash
sudo systemctl start mongod
```

### 4. تشغيل التطبيق
```bash
npm start
```

### 5. فتح المتصفح
افتح المتصفح على: `http://localhost:3000`

---

## 📝 ملاحظات مهمة

### إعدادات قاعدة البيانات
- تأكد من تشغيل MongoDB قبل بدء التطبيق
- الإعدادات موجودة في ملف `.env`

### إنشاء حساب مدير
للحصول على صلاحيات المدير، استخدم MongoDB Compass أو mongo shell:

```javascript
use ecole-chebbi

db.users.insertOne({
  name: "المدير",
  email: "admin@ecole-chebbi.tn",
  password: "$2a$10$...", // استخدم bcrypt لتشفير كلمة المرور
  role: "admin",
  isActive: true,
  createdAt: new Date()
})
```

أو سجّل حساب عادي ثم غيّر `role` إلى `admin` من قاعدة البيانات.

### التطوير مع التحديث التلقائي
```bash
# في نافذة terminal أولى - لمراقبة التطبيق
npm run dev

# في نافذة terminal ثانية - لمراقبة CSS
npm run build:css
```

---

## 🎯 الفضاءات المتاحة

### فضاء التلميذ
- تحميل الدروس
- عرض جدول الحصص
- التعليق على الدروس

### فضاء المدرس
- رفع الدروس (PDF, DOCX, PPTX)
- إدارة الدروس
- متابعة الإحصائيات

### فضاء الولي
- إرسال الملاحظات
- متابعة الأبناء
- استقبال الردود

### فضاء الإدارة
- إدارة المستخدمين
- الموافقة على التعليقات
- الرد على الملاحظات
- عرض الإحصائيات

---

## 🔧 حل المشاكل الشائعة

### المشكلة: لا يعمل MongoDB
**الحل:** تأكد من تثبيت وتشغيل MongoDB

### المشكلة: خطأ في CSS
**الحل:** قم ببناء CSS مرة أخرى
```bash
npx tailwindcss -i ./public/css/input.css -o ./public/css/output.css
```

### المشكلة: خطأ في رفع الملفات
**الحل:** تأكد من وجود مجلدات uploads
```bash
mkdir -p uploads/lessons
mkdir -p uploads/profile-pics
```

---

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل، افتح Issue على GitHub.

---

**بالتوفيق! 🎓✨**
