# ✅ خطوات التشغيل - المدرسة الإعدادية أبو القاسم الشابي

## 🔥 البدء السريع (4 خطوات فقط)

### 1️⃣ تثبيت المكتبات
```powershell
npm install
```

### 2️⃣ بناء ملفات CSS
```powershell
npx tailwindcss -i ./public/css/input.css -o ./public/css/output.css
```

### 3️⃣ إعداد قاعدة بيانات MySQL

#### أ. إنشاء قاعدة البيانات
```powershell
mysql -u root -p
# أدخل كلمة المرور: loi123
```

ثم في MySQL:
```sql
CREATE DATABASE ecole_chebbi CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;
```

#### ب. تأكد من ملف .env
```env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ecole_chebbi
DB_USER=root
DB_PASSWORD=loi123
```

### 4️⃣ تشغيل التطبيق
```powershell
npm start
```

**التطبيق سيُنشئ الجداول تلقائياً عند التشغيل الأول!**

**🌐 افتح المتصفح:** `http://localhost:3000`

---

## 🚂 النشر على Railway

راجع ملف **RAILWAY.md** للتعليمات الكاملة للنشر على Railway

**خطوات سريعة:**
1. أنشئ حساب على [Railway.app](https://railway.app)
2. ارفع المشروع على GitHub
3. اربط المستودع مع Railway
4. أضف MongoDB من Railway Marketplace
5. أضف المتغيرات البيئية
6. انشر التطبيق!

---

## 🔐 إنشاء حساب المدير (اختياري)

```powershell
npm run create-admin
```

سيتم إنشاء حساب بالبيانات التالية:
- **البريد الإلكتروني:** admin@ecole-chebbi.tn
- **كلمة المرور:** admin123

---

## 🛠️ الأوامر المفيدة

```powershell
# التشغيل مع التحديث التلقائي (للتطوير)
npm run dev

# بناء CSS مع المراقبة التلقائية
npm run build:css

# إنشاء حساب مدير
npm run create-admin
```

---

## ✨ جاهز للاستخدام!

**الصفحة الرئيسية:** http://localhost:3000  
**تسجيل الدخول:** http://localhost:3000/auth/login  
**إنشاء حساب:** http://localhost:3000/auth/register

---

**ننشئ جيل الغد بالعلم والمعرفة** 🎓
