<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="/css/output.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50">
  <%- include('../partials/navbar') %>
  
  <section class="py-8">
    <div class="container mx-auto px-4">
      <!-- الترحيب -->
      <div class="mb-8">
        <h1 class="text-4xl font-bold text-gray-800 mb-2">
          <i class="fas fa-user-graduate ml-2 text-primary-600"></i>
          مرحباً <%= user.name %>
        </h1>
        <div class="flex flex-wrap gap-4 text-gray-600">
          <p>
            <i class="fas fa-graduation-cap ml-1 text-primary-600"></i>
            القسم: <span class="font-semibold text-primary-600"><%= user.studentClass || 'غير محدد' %></span>
          </p>
          <% if (user.classNumber) { %>
          <p>
            <i class="fas fa-users ml-1 text-green-600"></i>
            الصف: <span class="font-semibold text-green-600"><%= user.classNumber %></span>
          </p>
          <% } %>
        </div>
      </div>
      
      <%- include('../partials/messages') %>
      
      <!-- الإحصائيات -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <div class="card p-6 hover:shadow-xl transition-shadow">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-gray-600 mb-1">إجمالي الدروس</p>
              <p class="text-4xl font-bold text-primary-600"><%= stats.totalLessons %></p>
            </div>
            <div class="text-5xl text-primary-200">
              <i class="fas fa-book"></i>
            </div>
          </div>
        </div>
        
        <div class="card p-6 hover:shadow-xl transition-shadow">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-gray-600 mb-1">تعليقاتي</p>
              <p class="text-4xl font-bold text-green-600"><%= stats.myComments %></p>
            </div>
            <div class="text-5xl text-green-200">
              <i class="fas fa-comments"></i>
            </div>
          </div>
        </div>
        
        <div class="card p-6 hover:shadow-xl transition-shadow">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-gray-600 mb-1">إشعارات جديدة</p>
              <p class="text-4xl font-bold text-orange-600"><%= stats.notifications %></p>
            </div>
            <div class="text-5xl text-orange-200">
              <i class="fas fa-bell"></i>
            </div>
          </div>
        </div>
      </div>
      
      <!-- الروابط السريعة -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
        <a href="/student/lessons" class="card p-6 hover:shadow-xl transition-all transform hover:-translate-y-1">
          <div class="text-center">
            <div class="text-5xl text-primary-600 mb-4">
              <i class="fas fa-books"></i>
            </div>
            <h3 class="text-xl font-bold text-gray-800 mb-2">عرض جميع الدروس</h3>
            <p class="text-gray-600">تصفح جميع الدروس المتاحة لقسمك</p>
          </div>
        </a>
        
        <a href="/student/schedules" class="card p-6 hover:shadow-xl transition-all transform hover:-translate-y-1">
          <div class="text-center">
            <div class="text-5xl text-purple-600 mb-4">
              <i class="fas fa-calendar-alt"></i>
            </div>
            <h3 class="text-xl font-bold text-gray-800 mb-2">الجدول المدرسي وروزنامة الفروض التأليفية</h3>
            <p class="text-gray-600">جداول أوقات الدروس والامتحانات</p>
          </div>
        </a>
        
        <a href="/student/grades" class="card p-6 hover:shadow-xl transition-all transform hover:-translate-y-1">
          <div class="text-center">
            <div class="text-5xl text-green-600 mb-4">
              <i class="fas fa-graduation-cap"></i>
            </div>
            <h3 class="text-xl font-bold text-gray-800 mb-2">نتائجي</h3>
            <p class="text-gray-600">عرض نتائجك ومعدلاتك</p>
          </div>
        </a>
        
        <a href="/student/notifications" class="card p-6 hover:shadow-xl transition-all transform hover:-translate-y-1">
          <div class="text-center">
            <div class="text-5xl text-orange-600 mb-4 relative">
              <i class="fas fa-bell"></i>
              <% if (stats.notifications > 0) { %>
                <span class="absolute top-0 right-0 bg-red-500 text-white text-xs rounded-full w-6 h-6 flex items-center justify-center">
                  <%= stats.notifications %>
                </span>
              <% } %>
            </div>
            <h3 class="text-xl font-bold text-gray-800 mb-2">الإشعارات</h3>
            <p class="text-gray-600">تحقق من آخر الإشعارات</p>
          </div>
        </a>
      </div>
      
      <!-- آخر الدروس -->
      <div class="card">
        <div class="p-6 border-b border-gray-200 flex justify-between items-center">
          <h2 class="text-2xl font-bold text-gray-800">
            <i class="fas fa-clock ml-2 text-primary-600"></i>
            آخر الدروس المضافة
          </h2>
          <a href="/student/lessons" class="text-primary-600 hover:text-primary-800 font-semibold">
            عرض الكل <i class="fas fa-arrow-left mr-2"></i>
          </a>
        </div>
        
        <div class="p-6">
          <% if (lessons.length === 0) { %>
            <div class="text-center py-12">
              <i class="fas fa-inbox text-6xl text-gray-300 mb-4"></i>
              <p class="text-xl text-gray-500">لا توجد دروس متاحة حالياً</p>
            </div>
          <% } else { %>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <% lessons.forEach(lesson => { %>
                <div class="border border-gray-200 rounded-lg p-5 hover:shadow-lg transition-shadow">
                  <div class="flex items-start justify-between mb-3">
                    <h3 class="text-lg font-bold text-gray-800 flex-1">
                      <%= lesson.title %>
                    </h3>
                    <span class="bg-primary-100 text-primary-800 px-3 py-1 rounded-full text-sm font-semibold mr-2">
                      <%= lesson.subject %>
                    </span>
                  </div>
                  
                  <p class="text-gray-600 mb-4 line-clamp-2"><%= lesson.description %></p>
                  
                  <div class="flex items-center justify-between text-sm text-gray-500 mb-4">
                    <div class="flex items-center">
                      <i class="fas fa-user-tie ml-2 text-primary-600"></i>
                      <%= lesson.teacher.name %>
                    </div>
                    <div class="flex items-center space-x-reverse space-x-4">
                      <span><i class="fas fa-eye ml-1"></i> <%= lesson.views %></span>
                      <span><i class="fas fa-download ml-1"></i> <%= lesson.downloads %></span>
                    </div>
                  </div>
                  
                  <div class="flex space-x-reverse space-x-3">
                    <a href="/student/lesson/<%= lesson.id %>" class="flex-1 btn-primary text-center">
                      <i class="fas fa-eye ml-2"></i>
                      عرض التفاصيل
                    </a>
                    <a href="/student/download/<%= lesson.id %>" class="btn-secondary">
                      <i class="fas fa-download"></i>
                    </a>
                  </div>
                </div>
              <% }); %>
            </div>
          <% } %>
        </div>
      </div>
      
      <!-- الإشعارات الأخيرة -->
      <% if (notifications.length > 0) { %>
        <div class="card mt-8">
          <div class="p-6 border-b border-gray-200">
            <h2 class="text-2xl font-bold text-gray-800">
              <i class="fas fa-bell ml-2 text-orange-600"></i>
              إشعارات جديدة
            </h2>
          </div>
          <div class="divide-y divide-gray-200">
            <% notifications.forEach(notif => { %>
              <div class="p-4 hover:bg-gray-50 transition-colors">
                <div class="flex items-start">
                  <div class="flex-shrink-0 ml-4">
                    <div class="w-10 h-10 rounded-full bg-orange-100 flex items-center justify-center">
                      <i class="fas fa-bell text-orange-600"></i>
                    </div>
                  </div>
                  <div class="flex-1">
                    <p class="text-gray-800 font-semibold mb-1"><%= notif.title %></p>
                    <p class="text-gray-600 text-sm mb-2"><%= notif.message %></p>
                    <p class="text-gray-400 text-xs">
                      <i class="fas fa-clock ml-1"></i>
                      <%= new Date(notif.createdAt).toLocaleDateString('fr-FR') %>
                    </p>
                  </div>
                </div>
              </div>
            <% }); %>
          </div>
          <div class="p-4 bg-gray-50 text-center">
            <a href="/student/notifications" class="text-primary-600 hover:text-primary-800 font-semibold">
              عرض جميع الإشعارات <i class="fas fa-arrow-left mr-2"></i>
            </a>
          </div>
        </div>
      <% } %>
    </div>
  </section>
  
  <%- include('../partials/footer') %>
  <script src="/js/main.js"></script>
</body>
</html>
