<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - المدرسة الإعدادية أبو القاسم الشابي</title>
  <link href="/css/output.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <section class="min-h-screen bg-gray-100 py-8">
    <div class="container mx-auto px-4">
      <div class="mb-6">
        <h1 class="text-3xl font-bold text-gray-800 mb-2">
          <i class="fas fa-child ml-2 text-primary-600"></i>
          نتائج الابن/الابنة
        </h1>
        <% if (childName) { %>
          <p class="text-gray-600">
            <i class="fas fa-user ml-2"></i>
            <%= childName %>
          </p>
        <% } %>
      </div>

      <%- include('../partials/messages') %>

      <% if (!hasChild) { %>
        <!-- نموذج البحث عن الابن -->
        <div class="card p-6 mb-6">
          <h2 class="text-xl font-bold text-gray-800 mb-4">
            <i class="fas fa-search ml-2 text-primary-600"></i>
            البحث عن نتائج الابن/الابنة
          </h2>
          <form method="GET" action="/parent/grades" class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <div>
              <label class="block text-gray-700 font-semibold mb-2">الاسم الأول للابن</label>
              <input 
                type="text" 
                name="searchFirstName" 
                class="input-field" 
                placeholder="الاسم الأول"
                value="<%= searchFirstName || '' %>"
                required
              >
            </div>
            
            <div>
              <label class="block text-gray-700 font-semibold mb-2">اللقب للابن</label>
              <input 
                type="text" 
                name="searchLastName" 
                class="input-field" 
                placeholder="اللقب"
                value="<%= searchLastName || '' %>"
                required
              >
            </div>
            
            <div class="flex items-end">
              <button type="submit" class="btn-primary w-full">
                <i class="fas fa-search ml-2"></i>
                بحث
              </button>
            </div>
          </form>
          
          <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mt-4">
            <p class="text-sm text-gray-700">
              <i class="fas fa-info-circle ml-2 text-blue-600"></i>
              <strong>ملاحظة:</strong> يجب إدخال الاسم الأول واللقب بالضبط كما هو مسجل في النظام
            </p>
          </div>
        </div>
        
        <!-- تنبيه: لم يتم ربط الحساب بابن -->
        <% if (!searchFirstName && !searchLastName) { %>
        <div class="card p-8 text-center">
          <i class="fas fa-link-slash text-6xl text-orange-400 mb-4"></i>
          <h2 class="text-2xl font-bold text-gray-800 mb-3">لم يتم ربط حسابك بابنك</h2>
          <p class="text-gray-600 mb-6">
            يمكنك البحث عن نتائج ابنك باستخدام النموذج أعلاه، أو تواصل مع الإدارة لربط حسابك بشكل دائم
          </p>
          <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 max-w-md mx-auto text-right">
            <h3 class="font-bold text-gray-800 mb-2">كيفية ربط الحساب الدائم:</h3>
            <ol class="text-gray-700 space-y-2 mr-6" style="list-style: arabic-indic;">
              <li>تواصل مع إدارة المدرسة</li>
              <li>قدم طلب لربط حسابك باسم ابنك/ابنتك</li>
              <li>سيقوم المسؤول بإضافة اسم الابن إلى حسابك</li>
              <li>بعدها ستتمكن من عرض النتائج مباشرة دون البحث</li>
            </ol>
          </div>
        </div>
        <% } %>
      <% } %>
      
      <% if (hasChild || (searchFirstName && searchLastName)) { %>
        <!-- المعدل العام -->
        <% if (overallAverage > 0) { %>
          <div class="card p-8 mb-8 text-center">
            <h2 class="text-gray-700 mb-3 text-xl">المعدل العام</h2>
            <div class="text-6xl font-bold mb-2 <%= overallAverage >= 10 ? 'text-green-600' : 'text-red-600' %>">
              <%= overallAverage.toFixed(2) %> / 20
            </div>
            <% if (overallAverage >= 16) { %>
              <p class="text-green-600 text-xl mt-2">
                <i class="fas fa-star ml-2"></i>
                أداء ممتاز!
              </p>
            <% } else if (overallAverage >= 14) { %>
              <p class="text-blue-600 text-xl mt-2">
                <i class="fas fa-thumbs-up ml-2"></i>
                أداء جيد جدًا
              </p>
            <% } else if (overallAverage >= 12) { %>
              <p class="text-yellow-600 text-xl mt-2">
                <i class="fas fa-smile ml-2"></i>
                أداء جيد
              </p>
            <% } else if (overallAverage >= 10) { %>
              <p class="text-orange-600 text-xl mt-2">
                <i class="fas fa-info-circle ml-2"></i>
                أداء متوسط - يحتاج متابعة
              </p>
            <% } else { %>
              <p class="text-red-600 text-xl mt-2">
                <i class="fas fa-exclamation-triangle ml-2"></i>
                يحتاج متابعة مكثفة
              </p>
            <% } %>
          </div>
        <% } %>

        <!-- فلتر الفصل -->
        <div class="card p-4 mb-6">
          <form method="GET" action="/parent/grades" class="flex items-center gap-4">
            <% if (searchFirstName && searchLastName) { %>
              <input type="hidden" name="searchFirstName" value="<%= searchFirstName %>">
              <input type="hidden" name="searchLastName" value="<%= searchLastName %>">
            <% } %>
            <label class="text-gray-700 font-semibold">الفصل:</label>
            <select name="semester" class="input-field flex-1" onchange="this.form.submit()">
              <option value="">جميع الفصول</option>
              <option value="الفصل الأول" <%= selectedSemester === 'الفصل الأول' ? 'selected' : '' %>>الفصل الأول</option>
              <option value="الفصل الثاني" <%= selectedSemester === 'الفصل الثاني' ? 'selected' : '' %>>الفصل الثاني</option>
              <option value="الفصل الثالث" <%= selectedSemester === 'الفصل الثالث' ? 'selected' : '' %>>الفصل الثالث</option>
            </select>
          </form>
        </div>

        <% if (Object.keys(gradesBySubject).length === 0) { %>
          <!-- لا توجد نتائج -->
          <div class="card p-12 text-center">
            <i class="fas fa-clipboard-list text-8xl text-gray-300 mb-6"></i>
            <h2 class="text-2xl font-bold text-gray-700 mb-2">لا توجد نتائج منشورة بعد</h2>
            <p class="text-gray-500">سيتم عرض النتائج هنا عندما يقوم المعلم بنشرها</p>
          </div>
        <% } else { %>
          <!-- النتائج حسب المادة -->
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
            <% Object.keys(gradesBySubject).forEach(subject => { 
              const subjectGrades = gradesBySubject[subject];
              const subjectAverage = subjectAverages[subject];
            %>
              <div class="card">
                <div class="p-6 border-b border-gray-200 bg-gradient-to-l from-primary-50 to-white">
                  <div class="flex justify-between items-center">
                    <h3 class="text-2xl font-bold text-gray-800">
                      <i class="fas fa-book ml-2 text-primary-600"></i>
                      <%= subject %>
                    </h3>
                    <div class="text-left">
                      <p class="text-sm text-gray-600">المعدل</p>
                      <p class="text-2xl font-bold <%= parseFloat(subjectAverage) >= 10 ? 'text-green-600' : 'text-red-600' %>">
                        <%= parseFloat(subjectAverage).toFixed(2) %>
                      </p>
                    </div>
                  </div>
                </div>

                <div class="p-6">
                  <% 
                  // تجميع النتائج حسب الفصل الدراسي
                  const gradedBySemester = {};
                  subjectGrades.forEach(grade => {
                    if (!gradedBySemester[grade.semester]) {
                      gradedBySemester[grade.semester] = [];
                    }
                    gradedBySemester[grade.semester].push(grade);
                  });
                  
                  Object.keys(gradedBySemester).forEach(semester => { 
                    const semesterGrades = gradedBySemester[semester];
                  %>
                    <div class="mb-6 last:mb-0">
                      <h4 class="font-bold text-gray-700 mb-3 pb-2 border-b border-gray-200">
                        <%= semester %>
                      </h4>
                      
                      <div class="space-y-3">
                        <% semesterGrades.forEach(grade => { %>
                          <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg">
                            <div class="flex-1">
                              <div class="flex items-center gap-2">
                                <% if (grade.gradeType === 'فرض عادي') { %>
                                  <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded text-xs font-semibold">
                                    فرض عادي
                                  </span>
                                <% } else if (grade.gradeType === 'فرض تأليفي') { %>
                                  <span class="bg-purple-100 text-purple-800 px-2 py-1 rounded text-xs font-semibold">
                                    فرض تأليفي
                                  </span>
                                <% } else { %>
                                  <span class="bg-green-100 text-green-800 px-2 py-1 rounded text-xs font-semibold">
                                    شفاهي
                                  </span>
                                <% } %>
                                
                                <% if (grade.examDate) { %>
                                  <span class="text-xs text-gray-500">
                                    <i class="fas fa-calendar ml-1"></i>
                                    <%= new Date(grade.examDate).toLocaleDateString('fr-FR') %>
                                  </span>
                                <% } %>
                              </div>
                              
                              <% if (grade.remarks) { %>
                                <p class="text-sm text-gray-600 mt-1 mr-2">
                                  <i class="fas fa-comment ml-1"></i>
                                  <%= grade.remarks %>
                                </p>
                              <% } %>

                              <p class="text-xs text-gray-500 mt-1 mr-2">
                                <i class="fas fa-chalkboard-teacher ml-1"></i>
                                الأستاذ: <%= grade.teacherFirstName %> <%= grade.teacherLastName %>
                              </p>
                            </div>
                            
                            <div class="text-left">
                              <span class="text-2xl font-bold <%= grade.gradeValue >= grade.maxGrade * 0.5 ? 'text-green-600' : 'text-red-600' %>">
                                <%= grade.gradeValue %>
                              </span>
                              <span class="text-gray-500">/ <%= grade.maxGrade %></span>
                            </div>
                          </div>
                        <% }); %>
                      </div>
                    </div>
                  <% }); %>
                </div>
              </div>
            <% }); %>
          </div>

          <!-- إحصائيات إضافية -->
          <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mt-8">
            <div class="card p-6">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-gray-600 mb-1">عدد المواد</p>
                  <p class="text-3xl font-bold text-primary-600"><%= Object.keys(gradesBySubject).length %></p>
                </div>
                <div class="text-5xl text-primary-200">
                  <i class="fas fa-books"></i>
                </div>
              </div>
            </div>
            
            <div class="card p-6">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-gray-600 mb-1">عدد النتائج</p>
                  <p class="text-3xl font-bold text-blue-600"><%= totalGrades %></p>
                </div>
                <div class="text-5xl text-blue-200">
                  <i class="fas fa-clipboard-list"></i>
                </div>
              </div>
            </div>
            
            <div class="card p-6">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-gray-600 mb-1">أعلى علامة</p>
                  <% 
                    let maxGrade = 0;
                    Object.values(gradesBySubject).forEach(subjectGrades => {
                      subjectGrades.forEach(g => {
                        if (parseFloat(g.gradeValue) > maxGrade) maxGrade = parseFloat(g.gradeValue);
                      });
                    });
                  %>
                  <p class="text-3xl font-bold text-green-600"><%= maxGrade.toFixed(2) %></p>
                </div>
                <div class="text-5xl text-green-200">
                  <i class="fas fa-trophy"></i>
                </div>
              </div>
            </div>
          </div>

          <!-- نصائح للولي -->
          <div class="card p-6 mt-8 bg-gradient-to-l from-blue-50 to-white border-r-4 border-blue-500">
            <h3 class="text-xl font-bold text-gray-800 mb-3">
              <i class="fas fa-lightbulb ml-2 text-yellow-500"></i>
              نصائح لمتابعة تقدم ابنك/ابنتك
            </h3>
            <ul class="space-y-2 text-gray-700 mr-6" style="list-style: disc;">
              <li>تابع النتائج بشكل دوري وتحدث مع ابنك/ابنتك حول أدائه</li>
              <li>شجع على المواد التي يتفوق فيها واحتفل بالنجاحات</li>
              <li>ساعد في تحسين المواد التي تحتاج جهد إضافي</li>
              <li>تواصل مع المعلمين عند الحاجة لمعرفة تفاصيل أكثر</li>
              <li>وفر بيئة مناسبة للدراسة في المنزل</li>
            </ul>
          </div>
        <% } %>
      <% } %>
    </div>
  </section>

  <%- include('../partials/footer') %>
</body>
</html>
