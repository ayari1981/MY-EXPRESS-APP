<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - المدرسة الإعدادية أبو القاسم الشابي</title>
  <link href="/css/output.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <section class="min-h-screen bg-gray-100 py-8">
    <div class="container mx-auto px-4">
      <!-- العنوان -->
      <div class="mb-8">
        <h1 class="text-4xl font-bold text-gray-800 mb-2">
          <i class="fas fa-user-friends ml-2 text-primary-600"></i>
          لوحة تحكم ولي الأمر
        </h1>
        <p class="text-gray-600">مرحباً <%= user.name %></p>
      </div>

      <%- include('../partials/messages') %>

      <!-- زر إرسال ملاحظة جديدة -->
      <div class="mb-6">
        <a href="/parent/feedback" class="btn-primary">
          <i class="fas fa-paper-plane ml-2"></i>
          إرسال ملاحظة جديدة
        </a>
      </div>

      <!-- الملاحظات المرسلة -->
      <div class="bg-white rounded-lg shadow-md overflow-hidden">
        <div class="p-6 border-b border-gray-200">
          <h2 class="text-2xl font-bold text-gray-800">ملاحظاتي</h2>
        </div>

        <div class="p-6">
          <% if (feedbacks && feedbacks.length > 0) { %>
            <div class="space-y-4">
              <% feedbacks.forEach(feedback => { %>
                <div class="border border-gray-200 rounded-lg p-6">
                  <div class="flex justify-between items-start mb-4">
                    <div>
                      <h3 class="font-bold text-gray-800 text-lg mb-1">
                        <i class="fas fa-tag ml-1"></i>
                        <%= feedback.subject %>
                      </h3>
                      <p class="text-xs text-gray-400">
                        <i class="fas fa-clock ml-1"></i>
                        <%= new Date(feedback.created_at).toLocaleDateString('ar-TN') %>
                      </p>
                    </div>
                    <% if (feedback.status === 'pending') { %>
                      <span class="px-3 py-1 bg-yellow-100 text-yellow-800 text-xs font-semibold rounded-full">
                        قيد المراجعة
                      </span>
                    <% } else if (feedback.status === 'responded') { %>
                      <span class="px-3 py-1 bg-green-100 text-green-800 text-xs font-semibold rounded-full">
                        تم الرد
                      </span>
                    <% } %>
                  </div>

                  <div class="bg-gray-50 rounded-lg p-4 mb-4">
                    <p class="text-gray-700"><%= feedback.message %></p>
                  </div>

                  <% if (feedback.adminResponse) { %>
                    <div class="bg-blue-50 border-r-4 border-blue-500 rounded-lg p-4">
                      <h4 class="font-semibold text-blue-800 mb-2">
                        <i class="fas fa-reply ml-1"></i>
                        رد الإدارة:
                      </h4>
                      <p class="text-gray-700"><%= feedback.adminResponse %></p>
                      <% if (feedback.respondedAt) { %>
                        <p class="text-xs text-gray-500 mt-2">
                          تم الرد في: <%= new Date(feedback.respondedAt).toLocaleDateString('ar-TN') %>
                        </p>
                      <% } %>
                    </div>
                  <% } %>
                </div>
              <% }) %>
            </div>
          <% } else { %>
            <div class="text-center py-12">
              <i class="fas fa-inbox text-6xl text-gray-300 mb-4"></i>
              <h3 class="text-xl font-bold text-gray-600 mb-2">لا توجد ملاحظات</h3>
              <p class="text-gray-500 mb-4">لم ترسل أي ملاحظات بعد</p>
              <a href="/parent/feedback" class="btn-primary inline-block">
                <i class="fas fa-paper-plane ml-2"></i>
                إرسال ملاحظة الآن
              </a>
            </div>
          <% } %>
        </div>
      </div>

      <!-- معلومات إضافية -->
      <div class="mt-8 grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="bg-blue-50 rounded-lg p-6">
          <h3 class="font-bold text-blue-800 mb-3">
            <i class="fas fa-info-circle ml-2"></i>
            معلومات مفيدة
          </h3>
          <ul class="space-y-2 text-gray-700">
            <li><i class="fas fa-check-circle text-green-500 ml-2"></i> يتم مراجعة جميع الملاحظات من قبل الإدارة</li>
            <li><i class="fas fa-check-circle text-green-500 ml-2"></i> سيتم الرد على ملاحظاتك في أقرب وقت</li>
            <li><i class="fas fa-check-circle text-green-500 ml-2"></i> يمكنك متابعة حالة ملاحظاتك من هنا</li>
          </ul>
        </div>

        <div class="bg-green-50 rounded-lg p-6">
          <h3 class="font-bold text-green-800 mb-3">
            <i class="fas fa-envelope ml-2"></i>
            للتواصل
          </h3>
          <p class="text-gray-700 mb-2">
            إذا كانت لديك أي استفسارات أو ملاحظات، لا تتردد في التواصل معنا
          </p>
          <a href="/parent/feedback" class="text-green-600 hover:text-green-800 font-semibold">
            إرسال ملاحظة <i class="fas fa-arrow-left mr-1"></i>
          </a>
        </div>
      </div>
    </div>
  </section>

  <%- include('../partials/footer') %>
</body>
</html>
