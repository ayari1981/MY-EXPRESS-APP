<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - المدرسة الإعدادية أبو القاسم الشابي</title>
  <link href="/css/output.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <section class="min-h-screen bg-gray-100 py-8">
    <div class="container mx-auto px-4">
      <!-- العنوان -->
      <div class="mb-8">
        <h1 class="text-4xl font-bold text-gray-800 mb-2">لوحة تحكم الإدارة</h1>
        <p class="text-gray-600">مرحباً <%= user.name %> - نظرة عامة على النظام</p>
      </div>

      <%- include('../partials/messages') %>

    <!-- بطاقات الإحصائيات -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
      <!-- إجمالي المستخدمين -->
      <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-gray-500 text-sm mb-1">إجمالي المستخدمين</p>
            <h3 class="text-3xl font-bold text-blue-600"><%= stats.totalUsers %></h3>
          </div>
          <div class="bg-blue-100 p-3 rounded-full">
            <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
            </svg>
          </div>
        </div>
      </div>

      <!-- المعلمون -->
      <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-gray-500 text-sm mb-1">المعلمون</p>
            <h3 class="text-3xl font-bold text-green-600"><%= stats.totalTeachers %></h3>
          </div>
          <div class="bg-green-100 p-3 rounded-full">
            <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
            </svg>
          </div>
        </div>
      </div>

      <!-- التلاميذ -->
      <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-gray-500 text-sm mb-1">التلاميذ</p>
            <h3 class="text-3xl font-bold text-purple-600"><%= stats.totalStudents %></h3>
          </div>
          <div class="bg-purple-100 p-3 rounded-full">
            <svg class="w-8 h-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
            </svg>
          </div>
        </div>
      </div>

      <!-- الدروس -->
      <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-gray-500 text-sm mb-1">إجمالي الدروس</p>
            <h3 class="text-3xl font-bold text-orange-600"><%= stats.totalLessons %></h3>
          </div>
          <div class="bg-orange-100 p-3 rounded-full">
            <svg class="w-8 h-8 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
          </div>
        </div>
      </div>
    </div>

    <!-- الإشعارات والتنبيهات -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-8">
      <!-- التعليقات المعلقة -->
      <div class="bg-white rounded-lg shadow-md p-6">
        <div class="flex items-center justify-between mb-4">
          <h2 class="text-xl font-bold text-gray-800">التعليقات المعلقة</h2>
          <span class="bg-yellow-100 text-yellow-800 text-sm font-semibold px-3 py-1 rounded-full">
            <%= stats.pendingComments %>
          </span>
        </div>
        <% if (stats.pendingComments > 0) { %>
          <a href="/admin/comments" class="text-blue-600 hover:text-blue-800 flex items-center">
            عرض التعليقات المعلقة
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
            </svg>
          </a>
        <% } else { %>
          <p class="text-gray-500">لا توجد تعليقات معلقة</p>
        <% } %>
      </div>

      <!-- ملاحظات الأولياء المعلقة -->
      <div class="bg-white rounded-lg shadow-md p-6">
        <div class="flex items-center justify-between mb-4">
          <h2 class="text-xl font-bold text-gray-800">ملاحظات الأولياء</h2>
          <span class="bg-red-100 text-red-800 text-sm font-semibold px-3 py-1 rounded-full">
            <%= stats.pendingFeedbacks %>
          </span>
        </div>
        <% if (stats.pendingFeedbacks > 0) { %>
          <a href="/admin/feedbacks" class="text-blue-600 hover:text-blue-800 flex items-center">
            عرض الملاحظات المعلقة
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
            </svg>
          </a>
        <% } else { %>
          <p class="text-gray-500">لا توجد ملاحظات معلقة</p>
        <% } %>
      </div>
    </div>

    <!-- آخر الأنشطة -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
      <!-- آخر الدروس -->
      <div class="bg-white rounded-lg shadow-md p-6">
        <h2 class="text-xl font-bold text-gray-800 mb-4">آخر الدروس المضافة</h2>
        <div class="space-y-3">
          <% if (recentLessons && recentLessons.length > 0) { %>
            <% recentLessons.forEach(lesson => { %>
              <div class="border-r-4 border-blue-500 pr-4 py-2">
                <h3 class="font-semibold text-gray-800"><%= lesson.title %></h3>
                <p class="text-sm text-gray-600">
                  المادة: <%= lesson.subject %> | القسم: <%= lesson.class %>
                </p>
                <p class="text-xs text-gray-500">
                  المعلم: <%= lesson.teacher ? lesson.teacher.name : 'غير محدد' %>
                </p>
              </div>
            <% }) %>
          <% } else { %>
            <p class="text-gray-500">لا توجد دروس حتى الآن</p>
          <% } %>
        </div>
      </div>

      <!-- آخر المستخدمين -->
      <div class="bg-white rounded-lg shadow-md p-6">
        <h2 class="text-xl font-bold text-gray-800 mb-4">آخر المستخدمين المسجلين</h2>
        <div class="space-y-3">
          <% if (recentUsers && recentUsers.length > 0) { %>
            <% recentUsers.forEach(u => { %>
              <div class="border-r-4 border-green-500 pr-4 py-2">
                <h3 class="font-semibold text-gray-800"><%= u.name %></h3>
                <p class="text-sm text-gray-600"><%= u.email %></p>
                <p class="text-xs text-gray-500">
                  الدور: 
                  <% if (u.role === 'teacher') { %>
                    <span class="text-green-600">معلم</span>
                  <% } else if (u.role === 'student') { %>
                    <span class="text-purple-600">تلميذ</span>
                  <% } else if (u.role === 'parent') { %>
                    <span class="text-blue-600">ولي أمر</span>
                  <% } else if (u.role === 'admin') { %>
                    <span class="text-red-600">مدير</span>
                  <% } %>
                </p>
              </div>
            <% }) %>
          <% } else { %>
            <p class="text-gray-500">لا يوجد مستخدمون حتى الآن</p>
          <% } %>
        </div>
      </div>
    </div>

    <!-- روابط سريعة -->
    <div class="mt-8 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      <a href="/admin/users" class="bg-gradient-to-br from-blue-500 to-blue-600 text-white rounded-lg shadow-md p-6 hover:shadow-lg transition-all transform hover:scale-105">
        <div class="flex items-center justify-between">
          <div>
            <h3 class="text-lg font-bold mb-1">إدارة المستخدمين</h3>
            <p class="text-sm opacity-90">عرض وإدارة جميع المستخدمين</p>
          </div>
          <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
          </svg>
        </div>
      </a>

      <a href="/admin/schedules" class="bg-gradient-to-br from-blue-500 to-blue-600 text-white rounded-lg shadow-md p-6 hover:shadow-lg transition-all transform hover:scale-105">
        <div class="flex items-center justify-between">
          <div>
            <h3 class="text-lg font-bold mb-1">إدارة الجداول</h3>
            <p class="text-sm opacity-90">رفع وتعديل جداول الأوقات</p>
          </div>
          <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
          </svg>
        </div>
      </a>

      <a href="/logs" class="bg-gradient-to-br from-purple-500 to-purple-600 text-white rounded-lg shadow-md p-6 hover:shadow-lg transition-all transform hover:scale-105">
        <div class="flex items-center justify-between">
          <div>
            <h3 class="text-lg font-bold mb-1">سجلات النشاط</h3>
            <p class="text-sm opacity-90">عرض سجلات نشاط المدراء</p>
          </div>
          <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
          </svg>
        </div>
      </a>

      <a href="/admin/comments" class="bg-gradient-to-br from-green-500 to-green-600 text-white rounded-lg shadow-md p-6 hover:shadow-lg transition-all transform hover:scale-105">
        <div class="flex items-center justify-between">
          <div>
            <h3 class="text-lg font-bold mb-1">إدارة التعليقات</h3>
            <p class="text-sm opacity-90">مراجعة والموافقة على التعليقات</p>
          </div>
          <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
          </svg>
        </div>
      </a>
    </div>
  </div>
</section>

<%- include('../partials/footer') %>
</body>
</html>
