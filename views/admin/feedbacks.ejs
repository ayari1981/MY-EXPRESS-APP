<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - المدرسة الإعدادية أبو القاسم الشابي</title>
  <link href="/css/output.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <section class="min-h-screen bg-gray-100 py-8">
    <div class="container mx-auto px-4">
      <!-- العنوان -->
      <div class="mb-8">
        <h1 class="text-4xl font-bold text-gray-800 mb-2">ملاحظات الأولياء</h1>
        <p class="text-gray-600">مراجعة والرد على ملاحظات أولياء الأمور</p>
      </div>

      <%- include('../partials/messages') %>

      <!-- الملاحظات -->
      <div class="space-y-4">
        <% if (feedbacks && feedbacks.length > 0) { %>
          <% feedbacks.forEach(feedback => { %>
            <div class="bg-white rounded-lg shadow-md p-6">
              <div class="flex justify-between items-start mb-4">
                <div>
                  <h3 class="font-bold text-gray-800 text-lg mb-1">
                    <%= feedback.parent ? feedback.parent.name : 'ولي أمر محذوف' %>
                  </h3>
                  <p class="text-sm text-gray-500">
                    <i class="fas fa-envelope ml-1"></i>
                    <%= feedback.parent ? feedback.parent.email : '-' %>
                  </p>
                  <p class="text-xs text-gray-400 mt-1">
                    <i class="fas fa-clock ml-1"></i>
                    <%= new Date(feedback.created_at).toLocaleDateString('fr-FR') %>
                  </p>
                </div>
                <% if (feedback.status === 'pending') { %>
                  <span class="px-3 py-1 bg-yellow-100 text-yellow-800 text-xs font-semibold rounded-full">
                    معلق
                  </span>
                <% } else if (feedback.status === 'responded') { %>
                  <span class="px-3 py-1 bg-green-100 text-green-800 text-xs font-semibold rounded-full">
                    تم الرد
                  </span>
                <% } %>
              </div>

              <div class="mb-4">
                <h4 class="font-semibold text-gray-700 mb-2">
                  <i class="fas fa-star ml-1"></i>
                  الموضوع: <%= feedback.subject %>
                </h4>
                <div class="bg-gray-50 rounded-lg p-4">
                  <p class="text-gray-700"><%= feedback.message %></p>
                </div>
              </div>

              <% if (feedback.adminResponse) { %>
                <div class="bg-blue-50 border-r-4 border-blue-500 rounded-lg p-4 mb-4">
                  <h4 class="font-semibold text-blue-800 mb-2">
                    <i class="fas fa-reply ml-1"></i>
                    ردك:
                  </h4>
                  <p class="text-gray-700"><%= feedback.adminResponse %></p>
                  <p class="text-xs text-gray-500 mt-2">
                    تم الرد في: <%= new Date(feedback.respondedAt).toLocaleDateString('fr-FR') %>
                  </p>
                </div>
              <% } else { %>
                <form method="POST" action="/admin/feedbacks/respond/<%= feedback.id %>">
                  <div class="mb-4">
                    <label class="block text-gray-700 font-semibold mb-2">
                      <i class="fas fa-reply ml-1"></i>
                      الرد على الملاحظة
                    </label>
                    <textarea 
                      name="response" 
                      class="input-field" 
                      rows="4" 
                      required
                      placeholder="اكتب ردك هنا..."
                    ></textarea>
                  </div>
                  <button type="submit" class="btn-primary">
                    <i class="fas fa-paper-plane ml-2"></i>
                    إرسال الرد
                  </button>
                </form>
              <% } %>
            </div>
          <% }) %>
        <% } else { %>
          <div class="bg-white rounded-lg shadow-md p-12 text-center">
            <i class="fas fa-inbox text-6xl text-gray-300 mb-4"></i>
            <h3 class="text-xl font-bold text-gray-600 mb-2">لا توجد ملاحظات</h3>
            <p class="text-gray-500">لم يتم استلام أي ملاحظات من أولياء الأمور بعد</p>
          </div>
        <% } %>
      </div>
    </div>
  </section>

  <%- include('../partials/footer') %>
</body>
</html>
